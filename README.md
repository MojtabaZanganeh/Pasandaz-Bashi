<div align="center">

![Next.js](https://img.shields.io/badge/Next.js-16-black?style=flat-square&logo=next.js)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue?style=flat-square&logo=typescript)
![Tailwind CSS](https://img.shields.io/badge/Tailwind_CSS-4-38B2AC?style=flat-square&logo=tailwind-css)
![Supabase](https://img.shields.io/badge/Supabase-PostgreSQL-3FCF8E?style=flat-square&logo=supabase)

**وب‌سایت:** [pasandazbashi.ir](https://pasandazbashi.ir)

یک اپلیکیشن تحت وب برای محاسبه معادل زمانی هزینه‌ها بر اساس درآمد شما

[ویژگی‌ها](#ویژگی‌ها) • [نصب و راه‌اندازی](#نصب-و-راه‌اندازی) • [پیکربندی](#پیکربندی) • [تکنولوژی‌ها](#تکنولوژی‌ها)

</div>

---

## ویژگی‌ها

### 💰 مدیریت درآمد
- **انواع درآمد:** ساعتی، روزانه، هفتگی، ماهانه، پروژه‌ای و سفارشی
- **محاسبه هوشمند:** برای درآمد هفتگی و ماهانه، تعداد روز کاری و ساعت کار در هر روز به صورت جداگانه وارد می‌شود
- **امنیت:** اطلاعات درآمد فقط در دستگاه کاربر ذخیره می‌شود و به سرور ارسال نمی‌گردد

### ⏱️ محاسبه معادل زمانی
- تبدیل هزینه‌ها به ساعت، روز، هفته و ماه کاری
- محاسبه بر اساس روزهای کاری واقعی شما (نه روزهای تقویمی)
- نمایش دقیق با اعداد فارسی

### 📊 گزارشات
- گزارش ماهانه صرفه‌جویی‌ها
- آمار کلی از ابتدا تاکنون
- تفکیک ماهانه با نمودار

### 🔄 همگام‌سازی
- ذخیره‌سازی آفلاین در مرورگر
- همگام‌سازی خودکار با سرور هنگام اتصال به اینترنت
- صف انتظار برای عملیات آفلاین

### 🔐 احراز هویت
- سیستم احراز هویت اختصاصی با نام کاربری و رمز عبور
- بدون نیاز به ایمیل
- ذخیره امن رمز عبور با bcrypt

---

## نصب و راه‌اندازی

### پیش‌نیازها

- [Node.js](https://nodejs.org/) نسخه ۱۸ به بالا
- [Bun](https://bun.sh/) (پیشنهادی) یا npm
- حساب کاربری [Supabase](https://supabase.com) (رایگان)

### مرحله ۱: کلون کردن پروژه

```bash
git clone https://github.com/your-username/pasandaz.git
cd pasandaz
```

### مرحله ۲: نصب وابستگی‌ها

```bash
bun install
# یا
npm install
```

### مرحله ۳: تنظیم متغیرهای محیطی

فایل `.env.local` را در ریشه پروژه ایجاد کنید:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# JWT Secret (برای احراز هویت)
JWT_SECRET=your_random_secret_key_at_least_32_characters
```

### مرحله ۴: اجرای پروژه

```bash
bun run dev
# یا
npm run dev
```

پروژه در آدرس `http://localhost:3000` قابل مشاهده است.

---

## پیکربندی Supabase

### مرحله ۱: ایجاد حساب کاربری

1. به [supabase.com](https://supabase.com) بروید
2. روی **Start your project** کلیک کنید
3. با GitHub یا ایمیل ثبت‌نام کنید

### مرحله ۲: ایجاد پروژه

1. روی **New Project** کلیک کنید
2. نام سازمان (Organization) را وارد کنید
3. نام پروژه را وارد کنید (مثلاً: `pasandaz`)
4. یک رمز عبور قوی برای دیتابیس انتخاب کنید
5. منطقه (Region) را انتخاب کنید (پیشنهادی: `Singapore` یا `Frankfurt`)
6. روی **Create new project** کلیک کنید
7. صبر کنید تا پروژه ساخته شود (حدود ۲ دقیقه)

### مرحله ۳: دریافت URL و API Key

1. در داشبورد پروژه، به بخش **Settings** > **API** بروید
2. مقادیر زیر را کپی کنید:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** (در بخش Project API keys) → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### مرحله ۴: ایجاد جداول دیتابیس

1. در داشبورد Supabase، به بخش **SQL Editor** بروید
2. محتوای فایل `supabase-schema.sql` را کپی کنید
3. روی **Run** کلیک کنید

این اسکریپت جداول زیر را ایجاد می‌کند:

| جدول | توضیحات |
|------|---------|
| `users` | کاربران (نام کاربری و رمز عبور) |
| `savings` | صرفه‌جویی‌های ثبت شده |

**نکته:** جدول `incomes` در این نسخه استفاده نمی‌شود چون اطلاعات درآمد فقط در دستگاه کاربر ذخیره می‌شود.

### مرحله ۵: تنظیم JWT Secret

یک رشته تصادفی حداقل ۳۲ کاراکتری برای `JWT_SECRET` تولید کنید:

```bash
# در لینوکس/مک
openssl rand -base64 32

# یا به صورت آنلاین از سایت‌هایی مثل:
# https://randomkeygen.com/
```

---

## ساختار پروژه

```
pasandaz/
   ├── app/
   │   ├── api/
   │   │   ├── auth/          # API های احراز هویت
   │   │   │   ├── login/     # ورود
   │   │   │   ├── logout/    # خروج
   │   │   │   ├── session/   # بررسی نشست
   │   │   │   └── signup/    # ثبت‌نام
   │   │   └── sync/          # همگام‌سازی داده‌ها
   │   ├── layout.tsx         # لایوت اصلی
   │   ├── page.tsx           # صفحه اصلی
   │   └── globals.css        # استایل‌های گلوبال
   ├── components/
   │   ├── app/               # کامپوننت‌های اپلیکیشن
   │   │   ├── AppLayout.tsx  # لایوت مشترک
   │   │   ├── HomePage.tsx   # صفحه اصلی
   │   │   ├── LoginPage.tsx  # صفحه ورود
   │   │   ├── OnboardingPage.tsx  # صفحه خوش‌آمدگویی
   │   │   ├── ReportsPage.tsx     # صفحه گزارشات
   │   │   ├── SettingsPage.tsx    # صفحه تنظیمات
   │   │   └── SignupPage.tsx      # صفحه ثبت‌نام
   │   └── ui/                # کامپوننت‌های UI (shadcn/ui)
   ├── lib/
   │   ├── auth.ts            # توابع احراز هویت (JWT, bcrypt)
   │   ├── supabase.ts        # کلاینت Supabase
   │   └── sync-service.ts    # سرویس همگام‌سازی
   ├── store/
   │   └── appStore.ts        # Zustand store
   ├── types/
   │   └── index.ts           # تایپ‌های TypeScript
   └── utils/
       └── time.ts            # توابع زمان و تبدیل
── public/
   ├── icons/                 # آیکون‌های PWA
   ├── manifest.json          # مانیفست PWA
   └── sw.js                  # Service Worker
── supabase-schema.sql        # اسکریپت ایجاد جداول
── package.json
```

---

## تکنولوژی‌ها

| تکنولوژی | کاربرد |
|----------|--------|
| [Next.js 16](https://nextjs.org/) | فریمورک React با App Router |
| [TypeScript](https://www.typescriptlang.org/) | تایپ‌سیفتی |
| [Tailwind CSS 4](https://tailwindcss.com/) | استایل‌دهی |
| [shadcn/ui](https://ui.shadcn.com/) | کامپوننت‌های UI |
| [Zustand](https://zustand-demo.pmnd.rs/) | مدیریت state |
| [Supabase](https://supabase.com/) | دیتابیس PostgreSQL |
| [bcryptjs](https://github.com/dcodeIO/bcrypt.js) | هش کردن رمز عبور |
| [jsonwebtoken](https://github.com/auth0/node-jsonwebtoken) | توکن JWT |

---

## اسکریپت‌های npm

```bash
# اجرای توسعه
bun run dev

# ساخت نسخه تولید
bun run build

# اجرای نسخه تولید
bun run start

# بررسی lint
bun run lint
```

---

## مشارکت

مشارکت‌ها همیشه خوشایند هستند! برای مشارکت:

1. Fork کنید
2. برنچ جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add amazing feature'`)
4. به برنچ push کنید (`git push origin feature/amazing-feature`)
5. Pull Request بزنید

---

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.

---

<div align="center">

**[pasandazbashi.ir](https://pasandazbashi.ir)**

</div>
